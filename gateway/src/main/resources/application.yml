spring:
  application:
    name: 'Gateway'
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8070
zuul:
  routes:
    ContentManagementService: 'cms/**'
