spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: 'ConfigServer'
      fail-fast: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/